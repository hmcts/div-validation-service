java:
    applicationPort: 4008
    environment:
        REFORM_SERVICE_NAME: "vs"
        REFORM_TEAM: "div"
        SLOT: "PRODUCTION"
        
    # Don't modify below here
    image: ${IMAGE_NAME}
    ingressHost: ${SERVICE_FQDN}